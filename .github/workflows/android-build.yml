name: Build Real Android Game

on:
  push:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Java
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        
    - name: Setup Android
      uses: android-actions/setup-android@v3
      
    - name: Fix project structure
      run: |
        # ایجاد پوشه‌های ضروری
        mkdir -p app/src/main/java/com/yourcompany/game
        mkdir -p app/src/main/res/values
        
        # کپی فایل‌های جاوا به محل صحیح
        cp *.java app/src/main/java/com/yourcompany/game/ 2>/dev/null || true
        
    - name: Create missing Android files
      run: |
        # ایجاد AndroidManifest.xml
        cat > app/src/main/AndroidManifest.xml << 'EOF'
<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.yourcompany.game">
    
    <uses-permission android:name="android.permission.INTERNET" />
    
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Safina 3D Game"
        android:theme="@style/AppTheme">
        
        <activity
            android:name="com.yourcompany.game.GameEngine"
            android:exported="true"
            android:screenOrientation="landscape">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
EOF

        # ایجاد فایل strings.xml
        cat > app/src/main/res/values/strings.xml << 'EOF'
<resources>
    <string name="app_name">Safina 3D Game</string>
</resources>
EOF

    - name: Build APK
      run: |
        echo "🏗️ ساختن APK واقعی..."
        chmod +x gradlew
        ./gradlew assembleDebug --stacktrace
        
    - name: Upload Real APK
      uses: actions/upload-artifact@v4
      with:
        name: safina-game-apk
        path: app/build/outputs/apk/debug/*.apk
